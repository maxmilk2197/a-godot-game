# AGENTS.md — 代码与文件约定

> 本文件给 AI 编码助手（以及未来的自己）看：在本仓库写代码、建文件，一律按以下方式。
> 所有规则均提炼自现有代码（`脚本/全局/*.gd`、`脚本/主菜单/设置.gd` 等），有疑问以现有代码为准。

---

## 语言与命名

- **标识符全中文**：变量、函数、常量、信号、参数都用中文（`音乐音量`、`_确保总线()`、`设置路径`）。
  - 私有成员/函数加 `_` 前缀（`_读取中`、`_延迟保存()`）；临时局部变量也用中文（`值`、`总线`、`实例`）。
  - autoload 注册名用英文（`Audio`、`Settings`、`SceneNav`），脚本文件名用中文（`音量设置.gd`）。
  - 个别通用缩写保留英文（`cfg`、`tween`、`cmd`），跟随现有写法。
- **文件与目录全中文**：`脚本/全局/音量设置.gd`、`场景/主菜单/设置.tscn`、`资源/音频/…`。
  - 脚本按功能放子目录：`脚本/全局/`（autoload）、`脚本/主菜单/`、`脚本/家/`、`脚本/手机/`…
  - 场景与脚本同名同目录分类：`场景/主菜单/设置.tscn` ↔ `脚本/主菜单/设置.gd`。
- 新建 `.gd` 后不要手写 `.gd.uid`，编辑器下次扫描会自动生成。

---

## 注释规范

- **文件头**用 `## ====` 包裹的说明块：写明脚本身份、对应场景/注册名、职责、用法示例。模板：

  ```gdscript
  extends Node
  ## ============================================================
  ## 音量设置（全局唯一，autoload 注册名：Audio）
  ## 负责音乐/音效两条总线的音量调节与持久化保存。
  ## 用法：
  ##   Audio.音乐音量 = 0.8        # 0.0 ~ 1.0
  ## ============================================================
  ```

- **成员/函数说明**用 `##`（悬浮可见），解释"为什么"的细节才写注释；行内注释用 `#`。
- 长脚本用 `#region 名字 … #endregion` 或 `# ====` 分隔条分区（见 `按钮.gd`、`设置.gd`）。

---

## GDScript 风格

- 缩进用 **Tab**（Godot 默认），行尾不加分号。
- 尽量带类型标注：`func _延迟保存() -> void:`、`var 音乐音量: float = 1.0:`、`var cfg := ConfigFile.new()`。
- 节点引用用 `@onready var 音乐滑块: HSlider = $"音量区/音乐行/音量滑块"`；脚本内多用 `$"../相对路径"`。
- 属性 setter 语法（`set(值):`）配合自动应用/自动保存，见下。
- 打印日志用中文标签前缀：`print("[存档]","…")`、`printerr("[存档]","…")`。

---

## 既有模式（新代码要复用）

### 全局数据（autoload）模式 — 参考 `音量设置.gd`、`设置数据.gd`

- 可持久化属性写成 **setter 自动生效**：赋值即应用（改总线/改运行时状态）并触发保存。
- 保存要 **防抖/防读回写**：`_读取中` 守卫避免 `读取()` 时逐项写盘；高频改动（拖滑块）用
  one-shot Timer 延迟合并落盘，`_exit_tree()` 里补一次冲刷。
- 持久化用 `ConfigFile` 存 `user://xxx.cfg`，节/键名用中文（`cfg.set_value("音量", "音乐", …)`）；
  存档除外（`save` 用 JSON 存 `user://Saves/save_N.json`）。

### 场景与弹层模式 — 参考 `设置.gd`、`按钮.gd`

- **弹层叠加，不切场景**：`var 实例 = 场景.instantiate()` → `get_tree().current_scene.add_child(实例)`，
  关闭用 `queue_free()`（保住下层界面与音乐）。
- 跨场景引用集中走 `SceneNav`（`SceneNav.设置`…）；**必须用 `load()`，不要 `preload`**
  （会与主界面脚本形成循环引用，见 `场景导航.gd` 头注释）。
- UI 统一挂 `资源/主题/温馨主题.tres`；字体 `资源/字体/LXGWWenKaiMono-Medium.ttf`。

### 场景文件（.tscn）

- 节点用中文名；本项目的 .tscn 节点头带 `unique_id=`，子节点要写 `parent=`。
- 优先在编辑器里搭场景；需要手改 .tscn 时只做最小编辑（如给节点补一行 `bus = &"Music"`），
  不重排、不删 `unique_id`。
- 音频节点必须挂总线：音乐 `bus = &"Music"`，音效 `bus = &"SFX"`（总线定义在 `default_bus_layout.tres`）。

---

## 验证

- 引擎：Steam 版 Godot 4.7.2，路径
  `D:/steam/steamapps/common/Godot Engine/godot.windows.opt.tools.64.exe`。
- 无头导入检查：`--headless --path D:/sister --import`（退出时的 ObjectDB 泄漏告警可忽略）。
- 改完脚本可用无头运行冒烟：`--headless --path D:/sister --quit-after 180`。

---

## 其他

- README 为中英双语，新增大功能记得同步 `README.md` 与 Roadmap。
- 不动 `addons/dialogic/`（第三方插件）；Dialogic 相关配置在 `project.godot` 的 `[dialogic]` 段。
- git 提交信息跟随仓库现状（简短，如 `ma`、`~`），不主动 commit/push，除非用户要求。

# 养只妹妹 —— 项目说明（新手上手指南）

这是用 Godot 4.7 做的视觉小说 + 养成小游戏。本文帮你快速看懂项目、接手修改。

## 一、怎么运行游戏

1. 打开 Godot 4.7，点「导入」，选择本文件夹里的 `project.godot`
2. 按 **F5**（或点右上角播放按钮）运行游戏
3. 游戏入口场景：`res://场景/主菜单/主界面.tscn`（新游戏 → 初次见面对话 → 进卧室）

## 二、文件地图（改东西先看这里）

| 文件 | 管什么 |
|---|---|
| `project.godot` | 项目设置：自动加载（autoload）、对话变量注册。**别用记事本以外的方式乱改**（见下方坑） |
| `脚本/全局/存档.gd` | 存档系统（JSON，存在 `user://Saves/save_N.json`） |
| `脚本/全局/养成管理器.gd` | **养成核心**：妹妹的心情/好感/体力/金币/天数/时段/行动点，以及睡觉翻天、随机浮动 |
| `脚本/家/养成面板.gd` | 卧室界面：左上属性面板 + 下方行动栏按钮逻辑，对话数值同步 |
| `场景/家/家.tscn` | 卧室场景（布局都在这里，Godot 编辑器里可视编辑） |
| `脚本/家/主脚本.gd` | 卧室主逻辑：背景按白天/晚上切换、信号处理、开手机 |
| `对话/养成_交流.dtl` | 「交流」对话（4 个选项，各加不同好感/心情） |
| `对话/养成_摸摸.dtl` | 「摸摸」对话 |
| `脚本/手机/聊天数据.gd` | 手机「消息」App 的聊天数据（联系人是妹妹/阿云） |
| `脚本/全局/AI对话管理器.gd` | 手机聊天的 AI 接口（配置存 `user://aisettings.cfg`） |
| `场景/屋外/世界.tscn` + `脚本/屋外/世界.gd` | 屋外地图（有「回家」按钮回卧室） |
| `脚本/全局/养成测试.gd` + `场景/测试/养成测试.tscn` | 养成系统自动测试 |

## 三、养成系统怎么玩 / 怎么改

**数值在哪改**：`脚本/全局/养成管理器.gd` 顶部
- `心情/好感/体力`：0~100；`金币`：随便多少
- `游玩天数`：第几天；`时段`：白天/晚上；`剩余行动点`：每时段 3 次
- `睡觉恢复体力 = 70`：睡觉后体力恢复到不低于 70

**行动效果在哪改**：`脚本/家/养成面板.gd`
- 交流/摸摸：扣 1 行动点 → 播放对话（数值在对话文件里 set）
- 做家务：体力 -12~-18、金币 +15~+25（随机）
- 睡觉：翻天（白天↔晚上），晚上→白天天数 +1，行动点重置
- 随机浮动：`养成.随机浮动(基础值)` = 基础值 ±2

**对话数值在哪改**：`对话/养成_交流.dtl`、`养成_摸摸.dtl`
- 例：`set {好感} += 3` 表示好感加 3（实际会有 ±2 随机）
- 新对话文件要注册到 `project.godot` 的 `[dialogic]` 的 dtl 列表里

**对话和数值怎么联动**：进卧室时面板先把数值写进对话系统，对话里 `set` 改数时面板监听变化、加随机、存盘、刷新界面。全自动，不用管。

## 四、怎么跑测试

养成系统带自动测试（不用装插件）：

```
"D:\steam\steamapps\common\Godot Engine\godot.windows.opt.tools.64.exe" --headless --log-file user://test.log --path . res://场景/测试/养成测试.tscn
```

- 退出码 0 = 全过；1 = 有失败
- 测试用存档槽位 99，跑完自己删，不碰真实存档
- 改过养成逻辑后跑一遍，防止改坏

## 五、git 怎么用（重要！）

- 看现在改了什么：`git status`
- 提交存档：`git add -A` 然后 `git commit -m "说明改了啥"`
- 看历史：`git log --oneline`
- 后悔了想回退：`git reset --hard <提交号>`（**会丢掉之后所有改动，慎用**）
- 当前 HEAD 是游离状态（不在任何分支上），提交后想找回来：`git reflog`

## 六、常见坑（踩过很多次）

1. **`project.godot` 里的中文键名**：用工具写中文键（如 autoload 名「养成」）可能变成乱码。改完用 Godot 跑一次，报「养成 not declared」就是键坏了。修法：用二进制替换把双编码字节换回正确 UTF-8，或者干脆用英文键名。
2. **不要用 PowerShell 的 Get-Content/Set-Content 改含中文的文件**——会整文件乱码。用 Godot 编辑器或能正确写 UTF-8 的编辑器。
3. **headless 运行要带 `--log-file`**：这台机器上不带会启动崩溃（控制台输出的问题），带 `--log-file 路径` 就正常。
4. **对话里 set 的变量必须先注册**：`project.godot` 的 `[dialogic]` → `variables` 里要有 心情/好感/体力/金币，否则对话会报错。
5. **存档文件**：`user://` 在 Windows 上一般是 `C:\Users\<你>\AppData\Roaming\Godot\app_userdata\养只妹妹\`。想开新档就删里面的 `Saves` 文件夹。

## 七、这一版做了什么（2026-08-15）

- 手机「巨信」改名「消息」（10 处）
- 重做养成系统（JSON 存档）：属性面板 + 行动栏（交流/摸摸/做家务/睡觉/离开）
- 行动点机制、睡觉翻天（时段切换、天数推进、体力恢复）
- 对话联动：对话里 set 数值 → 自动同步回存档（双向）
- 删掉旧「信息」面板；点妹妹的旧菜单「交流」改名「聊天」
- 屋外世界加了「回家」按钮
- 修掉主脚本里跳「玄关」的坏引用（改成去屋外世界）
